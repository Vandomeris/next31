export default function UserInfo({ name, username, email, phone, website }) {
    return (
        <div>

            <p>{name}</p>
            <p>{username}</p>
            <p>{email}</p>
            <p>{phone}</p>
            <p>{website}</p>

        </div>
    )
}
